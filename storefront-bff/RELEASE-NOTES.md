# Release v1.0.0

First tagged release of the YAS CD pipeline. The CI builds this service as
`thuonghong/yas-storefront-bff:v1.0.0` on the `v1.0.0` git tag, which ArgoCD's
`staging-storefront-bff` Application tracks (see `cd/argocd/staging-apps.yaml`).
